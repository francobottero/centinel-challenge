rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /users/{userFolder}/receipts/{allPaths=**} {
      allow read, write: if request.auth != null
        && request.auth.token.storageFolder is string
        && userFolder == request.auth.token.storageFolder;
    }
  }
}
